from setuptools import setup

setup(
    name='colab_or_laptop',
    version='0.1.0',
    py_modules=['colab_or_laptop'],
    install_requires=[
        # List your project's dependencies here.
    ],
)
