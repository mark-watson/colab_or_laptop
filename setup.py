from setuptools import setup, find_packages

setup(
    name='colab_or_laptop',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # List your project's dependencies here.
    ],
)

